{% block select2_widget %}
    {% apply spaceless %}
    <select class="select2-enable" data-remote-target="{{ path('api_users') }}" {{ block('widget_attributes') }}>
    </select>
    {% endapply %}
{% endblock %}