<div class="head-wrap {{ css_prefix|default('') }} bg-primary" style="background-image: url({{ settings.get('style.bg_image') }})">
    <div class="head-content container">
        {% set logo = settings.get('style.logo') %}
        {% if logo is empty %}
            <img class="logo mb-4" src="{{ asset("build/images/logo-white.png") }}" alt="Logo">
        {% else %}
            <img class="logo mb-4" src="{{ logo }}" alt="Logo">
        {% endif %}
        {% block headerContent %}
        {% endblock %}
    </div>
</div>

{% block nav %}
    <div class="container">
        {%  include 'site/components/nav/navMain.html.twig' %}
    </div>
{% endblock %}