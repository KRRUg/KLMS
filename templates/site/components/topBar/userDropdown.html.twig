<div class="dropdown d-inline-block">
    <button type="button" id="dropdownMenu1" data-toggle="dropdown" class="btn btn-link dropdown-toggle">
        <i class="fas fa-user-circle"></i> {{ app.user.username }}
    </button>
    <div class="dropdown-menu dropdown-menu-right mt-0 p-0">
        <a class="dropdown-item" href="{{ path('user_profile') }}"><i class="fas fa-user"></i> Mein Profil</a>
        <a class="dropdown-item" href="#"><i class="fas fa-users"></i> Mein Clan</a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Abmelden</a>
    </div>
</div>