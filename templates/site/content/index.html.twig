{% extends 'site/siteBase.html.twig' %}

{% block title %}KLMS - {{ content.title }}{% endblock %}

{% block main %}
    <div class="container mb-5">
        <h1 >{{ content.title }}</h1>
        <p class="card-text">
            {{ content.content|raw }}
        </p>
    </div>
{% endblock %}
