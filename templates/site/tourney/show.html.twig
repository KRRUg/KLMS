{% extends 'site/siteBase.html.twig' %}

{% block title %}{{ settings.get('site.title') }} - {{ tourney.name }}{% endblock %}

{% block main %}
    <div class="container mb-5">
        <div class="card">
            <div class="card-body">
                <div class="card-title">
                    <h1>{{ tourney.name }}</h1>
                    <p>{{ tourney.description }}</p>
                </div>
                <div class="card-text">
                    <div class="tournament">
                        {% for round in tree %}
                        <div class="round">
                            {% for game in round %}
                                {% if game is not empty %}
                                    <div class="match">
                                        <div class="teams">
                                            <div class="team {{ game.hasWon(true) ? 'winner' : '' }}">
                                                {% if game.teamA is not null %}
                                                    <span class="team-name">{{ game.teamA.name | default(game.teamA.members[0].gamer | username)  }}</span>
                                                    <span class="team-score">{{ tourney.showPoints ? (game.scoreA() ?? '') : '' }}</span>
                                                {% else %}
                                                    <span class="team-name"></span>
                                                    <span class="team-score"></span>
                                                {% endif %}
                                            </div>
                                            <div class="team {{ game.hasWon(false) ? 'winner' : '' }}">
                                                {% if game.teamB is not null %}
                                                    <span class="team-name">{{ game.teamB.name | default(game.teamB.members[0].gamer | username)  }}</span>
                                                    <span class="team-score">{{ tourney.showPoints ? (game.scoreB() ?? '') : '' }}</span>
                                                {% else %}
                                                    <span class="team-name"></span>
                                                    <span class="team-score"></span>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                {% else %}
                                    <div class="match empty"></div>
                                {% endif %}
                            {% endfor %}
                        </div>
                        {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('tourney') }}
{% endblock %}