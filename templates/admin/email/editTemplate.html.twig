{% extends 'admin/adminBase.html.twig' %}

{% block title %}KLMS - Admin{% endblock %}
{% block siteheading %}<i class="far fa-newspaper"></i> News: Neue News{% endblock %}
{% block main %}

    {% include('admin/email/includes/menu.html.twig') %}
    <div class="example-wrapper">
        <div class="row">
            <div class="col-9">{{ form(form) }}</div>
            <div class="col-3">
                <h3>Verwendbare Felder</h3>

                {% for key,field in availableFields %}
                    <p>
                        &#123;&#123;{{ key }}&#125;&#125;<br>
                        <small>{{ field }}</small>
                    </p>
                {% endfor %}

            </div>
        </div>

    </div>
{% endblock %}
