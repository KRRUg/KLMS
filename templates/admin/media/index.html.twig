{% extends 'admin/adminBase.html.twig' %}

{% block title %}KLMS - Admin{% endblock %}
{% block siteheading %}<i class="far fa-file-image"></i> Media{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('media') }}
{% endblock %}
{% block main %}
    <style>
        .to-show {
            display: none;
        }
        .to-hover:hover > .to-show {
            display: block;
        }
    </style>

    <div class="row">
        <div class="col mb-3">
            <button type="button" class="btn btn-success" data-toggle="modal" data-target="#uploadModal">
                <i class="fas fa-plus"></i> Upload</a>
            </button>
        </div>
        <div class="col-md-auto mb-3">
            <div class="input-group">
                <div class="input-group-prepend">
                    <div class="input-group-text"><i class="fa fa-filter"></i></div>
                </div>
                <select id="filterSelect" class="mr-3">
                    <option value="all">Alles</option>
                    <option value="image">Bilder</option>
                    <option value="application">Dokumente</option>
                </select>
            </div>
        </div>
        <div class="col-md-auto mb-3">
            <div class="input-group">
                <div class="input-group-prepend">
                    <div class="input-group-text"><i class="fa fa-search"></i></div>
                </div>
                <input id="searchInput" type="text"/>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="border overflow-auto h-100 w-100">
            <div id="mediaList" class="row m-2">
                {% if media is empty %}
                    <p class="text-muted text-center w-100">Keine Medien hochgeladen.</p>
                {% else %}
                    {% for i in media %}
                        <div class="card mr-2 mb-2 to-hover" style="width: 250px;"
                             data-id="{{ i.id }}"
                             data-name="{{ i.media.originalName }}"
                             data-mime-type="{{ i.media.mimeType }}"
                        >
                            <div class="card-img-top border-bottom" style="height: 200px">
                            {% if i.isImage() %}
                                <img class="h-100 w-100 p-1" style="object-fit: contain;"
                                     src="{{ vich_uploader_asset(i, 'mediaFile') | imagine_filter('thumbnail_small') }}"
                                     alt="{{ i.media.name }}"
                                >
                            {% else %}
                                {% set icon = i.media.mimeType == "application/pdf" ? "fa-file-pdf" : "fa-file" %}
                                <div class="h-100 w-100 d-flex align-items-center justify-content-center">
                                    <i class="far {{ icon }} fa-6x text-black-50"></i>
                                </div>
                            {% endif %}
                            </div>
                            <div class="card-img-overlay text-right w-100 to-show">
{# <a href="#" data-toggle="popover" data-placement="bottom" data-trigger="focus" data-html="true" data-content="Wirklich löschen? <a href='{{ path('admin_media_delete', {'id' : i.id}) }}'>Ja</a>"><i class="fas fa-trash-alt"></i></a>#}
                                <a class="button-del" href='{{ path('admin_media_delete', {'id' : i.id}) }}'><i class="fas fa-trash-alt"></i></a>
                                <a class="ml-1" href="{{ path('media', {'name' : i.media.name} ) }}"><i class="fas fa-eye"></i></a>
                            </div>
                            <div class="card-body p-2 text-muted text-truncate text-center">
                                <span>{{ i.media.originalName }}</span>
                            </div>
                        </div>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </div>

    {% include 'admin/components/modals/form.html.twig' with {'modal_name': 'uploadModal', 'button_save': 'Upload', 'label_head': 'Upload', 'form': form_upload} %}
    {% include 'admin/components/modals/confirm.html.twig' with {'modal_name': 'confirmModal'} %}

{% endblock %}