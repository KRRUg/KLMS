{% extends 'admin/adminBase.html.twig' %}

{% block title %}KLMS - Admin{% endblock %}
{% block siteheading %}<i class="far fa-file-alt"></i> Content{% endblock %}

{% block main %}
    <div class="row mb-4">
        <div class="col-12">
            <button class="btn btn-success" id="new"><i class="fas fa-plus"></i> Neuer Content</button>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card">
                <table class="table" id="userTable">
                    <thead>
                    <tr>
                        <th class="col-md-1">Id</th>
                        <th class="col-md-8">Titel</th>
                        <th class="col-md-3">Aktiv</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for c in content %}
                        <tr>
                            <td>
                                {{ c.getId() }}
                            </td>
                            <td>
                                <a href="{{ path('admin_content_edit', {'id' : c.id}) }}">
                                    {{ c.getTitle() }}
                                </a><br><small class="text-muted">{{ c.getDescription() }}</small>
                            </td>
                            <td>
                                {% for nn in nav.getByContent(c) %}
                                    <span class="badge badge-secondary">{{ nn.getName() }}</span>
                                {% endfor %}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
